#!/usr/bin/env ruby

root_dir = File.expand_path('../..', __FILE__)
$LOAD_PATH.unshift(File.join(root_dir, 'lib'))

require 'anagram_trainer/game'

game = AnagramTrainer::Game.new(root_dir)
game.start
